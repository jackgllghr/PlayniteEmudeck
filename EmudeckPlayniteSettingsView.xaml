<UserControl x:Class="EmudeckPlaynite.EmudeckPlayniteSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <StackPanel>
        <TextBlock Text="Emudeck Install Directory:"/>
        <StackPanel Orientation="Horizontal" MinWidth="100%">
            <TextBox MinWidth="100%" Text="{Binding Settings.EmudeckInstallDir}"/>
            <!-- <Button>Browse</Button> -->
        </StackPanel>
    </StackPanel>
</UserControl>